/*
** EPITECH PROJECT, 2022
** background_manager.hpp
** File description:
** functions
*/

#include <raylib.h>
#include "raylib.hpp"
#include "background_manager_obj.hpp"

namespace Raylib {

    BackGroundObj::BackGroundObj(Engine &engine) : _engine(engine)
    {
    }

    void BackGroundObj::draw()
    {
    }

    void BackGroundObj::update()
    {
        //MyRectangle background(0, 0, 300, 300);
        
        //if (get_game_object(MainMenuObj) != nullptr)
            
        //else if (get_game_object(HowToPlayObj))
            
    }
}
